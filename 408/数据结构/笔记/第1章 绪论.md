<h2 align="center">第一章 绪论</h2>

> **408 高频考点提醒**：
> - **数据元素/数据项**：数据元素是数据的基本单位，数据项是构成数据元素的**不可分割的最小单位**
> - **数据结构三要素**：**逻辑结构、存储结构、数据的运算**
> - **逻辑结构四类型**：集合、线性 **1:1**、树形 **1:n**、图状 **n:n**
> - **存储结构**：顺序/链式/索引/散列
> - **算法五特性**：有穷性、确定性、可行性、输入、输出
> - **算法 ≠ 程序**：算法必须**可终止**，程序可以是无穷的
> - **时间复杂度**：加法规则**保留最高阶**、乘法规则直接相乘，阶排序**常对幂指阶**
> - **空间复杂度**：一般指**辅助空间**（输入数据外额外占用），原地 $S(n)=O(1)$

### （一）数据结构的基本概念

* **数据**：是信息的载体，是描述客观事物属性的数、字符及所有能输入到计算机中并被计算机程序识别和处理的符号的集合 。


* **数据元素与数据项**：数据元素是数据的基本单位，通常作为一个整体进行考虑和处理 。一个数据元素可由若干数据项组成，数据项是构成数据元素的不可分割的最小单位，对客观事物某一方面的数据描述 。


* **数据对象**：数据对象则是具有相同性质的数据元素的集合，是数据的一个子集 。


* **数据类型**：是一个值的集合和定义在此集合上的一组操作的总称 。
    * 原子类型：其值不可再分的类型
    * 结构类型：由若干个数据项组成的类型
* **抽象数据类型 (ADT)**：是抽象数据组织及与之相关的操作，它用数学化的语言定义数据的逻辑结构和运算，与具体的实现无关。

    * ADT=(D,S,P)

        * D：数据对象
        * S：D上的关系集
        * P：对D的基本操作集

* **数据结构**：是指相互之间存在一种或多种特定关系的数据元素的集合，它是数据类型及其相互关系的集合 。包含**逻辑结构、存储结构和数据的运算**。


### （二）数据结构的三要素

数据结构主要由以下三个要素构成：

1. #### **逻辑结构**（数据元素之间的逻辑关系）：

* **集合**：各个元素同属一个集合，别无其他关系 。
* **线性结构**：数据元素之间是一对一的关系 。
* **树形结构**：数据元素之间是一对多的关系 。
* **图状结构（网状结构）**：数据元素之间是多对多的关系 。

2. #### **物理结构 / 存储结构**（数据元素的表示及其相互的关系）：

* **顺序存储**：逻辑上相邻的元素存储在物理位置上也相邻的存储单元中 。
* **链式存储**：逻辑上相邻的元素在物理位置上可以不相邻，借助指示元素存储地址的指针来表示元素之间的逻辑关系 。
* **索引存储**：在存储元素信息的同时，还建立附加的索引表 。
* **散列存储**：根据元素的关键字直接计算出该元素的存储地址，又称哈希 (Hash) 存储 。

3. #### **数据的运算**：

* 包括运算的**定义**和**实现**。运算的定义针对逻辑结构，指出运算的功能；运算的实现针对存储结构，指出运算的具体操作步骤 。

### （三）算法的基本概念

* **核心理念**：程序 = 数据结构 + 算法 。算法是对特定问题求解步骤的一种描述，它是指令的有限序列，其中的每条指令表示一个或多个操作 。

* **描述算法的方法**：**伪代码（伪语言）**、**自然语言**、**程序设计语言**、**流程图**（传统流程图与 N-S 结构图）；考试中多用伪代码或程序设计语言 。（教材 1.2.1）

- **算法的五个特性**：

  1. **有穷性**：一个算法必须总在执行有穷步之后结束，且每一步都可在有穷时间内完成（注：程序可以是无穷的） 。

  2. **确定性**：算法中每条指令必须有确切的含义，相同的输入只会产生相同的输出 。

  3. **可行性**：算法中描述的操作都可以通过已经实现的基本运算执行有限次来实现 。
  
  
     4. **输入**：一个算法有零个或多个输入 。
  
  
     5. **输出**：一个算法有一个或多个输出 。
  

* **“好”算法的特质**：
  
    1. **正确性**：能够正确地解决求解问题 。
    
    2. **可读性**：具有良好的可读性，以帮助人们理解 。
    
    3. **健壮性**：输入非法数据时，算法能适当地做出反应或进行处理 。
    
     4. **通用性**：算法的处理结果对于一般的数据都成立
    
     5. **高效率与低存储量需求**：即算法执行省时、省内存（时间复杂度低、空间复杂度低） 。

### （四）算法的复杂度度量

#### 一、时间复杂度

衡量算法时间开销 $T(n)$与问题规模 $n$的关系 。

* **影响因素**：问题规模、数据初始状态、选用策略、程序设计语言、编译产码质量、机器速度（后四者归类为**软/硬件因素**）；撇开软硬件因素，算法运行工作量只依赖**问题规模 $n$** 。（教材 1.2.2）

* **计算规则**：
    * **加法规则**（保留最高阶）：$T(n) = O(f(n)) + O(g(n)) = O(max(f(n), g(n)))$。
    
    - **乘法规则**：$T(n) = O(f(n)) \times O(g(n)) = O(f(n) \times g(n))$。


* **杂度排序（常对幂指阶）**：$O(1) < O(log_2 n) < O(n) < O(n log_2 n) < O(n^2) < O(n^3) < O(2^n) < O(n!) < O(n^n)$。


* **三种时间复杂度**：
    * **最好时间复杂度**：考虑输入数据“最好”的情况 。
    
    
    * **最坏时间复杂度**：考虑输入数据“最坏”的情况 。
    
    
    * **平均时间复杂度**：考虑所有输入数据都等概率出现的情况下的期望运行时间 。
    

#### 二、空间复杂度

衡量算法空间开销（内存开销）与问题规模 $n$之间的关系 $S(n)$。

* **辅助空间**：程序运行所需存储空间一般包括指令/常数/变量、输入数据、辅助空间三方面；**一般地，算法的空间复杂度指的是辅助空间**，即除输入数据外**额外占用的空间**（王道/教材口径）。（教材 1.2.3）

* **算法原地工作**：无论问题规模怎么变，算法运行所需的内存空间都是固定的常量，即 $S(n) = O(1)$。


* **内存分析**：只需关注与问题规模相关的变量（如一维数组 $O(n)$，二维数组 $O(n^2)$） 。


* **函数递归调用**：递归程序的空间复杂度通常等于递归调用的深度 。

---

### （五）本章总结

**知识结构图**：

```
第一章 绪论
├─（一）数据结构的基本概念
│   └─ 数据/数据元素/数据项/数据对象/数据类型/ADT=(D,S,P)
├─（二）数据结构的三要素
│   ├─ 逻辑结构（集合/线性 1:1/树形 1:n/图状 n:n）
│   ├─ 存储结构（顺序/链式/索引/散列）
│   └─ 数据运算（定义 vs 实现）
├─（三）算法的基本概念
│   ├─ 描述方法（伪代码/自然语言/程序设计语言/流程图 N-S）
│   ├─ 五个特性（有穷/确定/可行/输入/输出）
│   └─ "好"算法（正确/可读/健壮/通用/高效率低存储）
└─（四）算法的复杂度度量
    ├─ 时间复杂度（影响因素、加乘规则、常对幂指阶、最好/最坏/平均）
    └─ 空间复杂度（辅助空间、原地 O(1)、一维 O(n)、二维 O(n²)、递归=深度）
```

**高频考点速记**：

| 考点 | 一句话记忆 |
|:---|:---|
| 数据 | 信息的载体，是**符号的集合** |
| 数据元素 vs 数据项 | 数据元素是**基本单位**；数据项是**不可分割的最小单位** |
| 数据对象 | 具有**相同性质**的数据元素的集合 |
| 数据类型 | **值的集合 + 定义在其上的一组操作**（原子/结构类型） |
| 抽象数据类型 ADT | ADT=(D,S,P)，用数学化语言定义，**与具体实现无关** |
| 数据结构 | 三要素：**逻辑结构、存储结构、数据运算** |
| 逻辑结构 | 集合/线性 **1:1**/树形 **1:n**/图状 **n:n** |
| 顺序存储 | 逻辑相邻的元素**物理位置也相邻** |
| 链式存储 | 逻辑相邻物理可不相邻，靠**指针**指示逻辑关系 |
| 索引存储 | 存储数据的同时**建立索引表** |
| 散列存储 | 按关键字**直接计算**存储地址（哈希存储） |
| 数据运算 | **定义**针对逻辑结构，**实现**针对存储结构 |
| 程序 = 数据结构 + 算法 | 算法是**指令的有限序列** |
| 算法描述方法 | **伪代码**/自然语言/程序设计语言/流程图（N-S） |
| 有穷性 | 算法必须有穷步内结束；**程序可以是无穷的** |
| 确定性 | 每条指令含义确切，相同输入必得**相同输出** |
| 可行性 | 操作可由**基本运算执行有限次**来实现 |
| 输入与输出 | **0 个或多个输入**、**1 个或多个输出** |
| "好"算法的特质 | 正确、可读、健壮、通用、**高效率与低存储** |
| 时间复杂度 | $T(n)$ 与规模 $n$ 的关系；加法规则**保留最高阶**、乘法规则直接相乘 |
| 复杂度排序 | **常对幂指阶**：$O(1)<O(\log_2 n)<O(n)<O(n\log_2 n)<O(n^2)<O(n^3)<O(2^n)<O(n!)$ |
| 三种时间复杂度 | **最好/最坏/平均**（平均需输入等概率出现） |
| 时间影响因素 | 规模/初值/策略/语言/编译质量/机器速度（软硬件），作业量只随规模 $n$ |
| 空间复杂度 | $S(n)$ 与规模 $n$ 的关系；一般指**辅助空间**（输入数据外额外占用） |
| 原地工作 | 所需内存为**常量**，$S(n)=O(1)$ |
| 数组空间 | 一维数组 **$O(n)$**、二维数组 **$O(n^2)$** |
| 递归空间 | 通常等于**递归调用的深度** |
