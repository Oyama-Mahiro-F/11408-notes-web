> 用法：做题后把错题号记在对应小节下，再把错因按主题沉淀成条目；每条漏洞下附对应的**错题原文**，复习时先遮住条目结论与答案、只看题自测。
>
> 题源：《考研计算机题型精练（组成原理）》第一部分 基础巩固篇（原文 `参考\考研计算机题型精练（计算机组成原理）.txt`）；小节号与错题号均按该部分编号。

## 计算机组成原理

### 1.1 计算机系统的基本组成

错题：3、6

**① 二进制表示的根本理由是物理器件（错题 3）**

- 计算机中所有信息仍以二进制表示，**理由是由物理器件的性能决定**：二进制只有 0/1 两个数字，恰好对应逻辑电路的高/低电平，实现方便、简单可靠
- 问"**理由/原因**"要答器件层面；"运算速度快""信息处理方便"是二进制带来的**结果**，不是采用它的原因

> **原题 3**：到目前为止，计算机中所有的信息仍以二进制方式表示的理由是（　）。
>
> A. 节约元件　B. 运算速度快　C. 由物理器件的性能决定　D. 信息处理方便
>
> 答案：**C**

**② 主存 = 程序运行时的存储位置（错题 6）**

- 标准表述：**主存（内存）是程序运行时的存储位置，包括所需的数据**
- 关键限制：**CPU 只能从主存取指令与操作数，不能直接与外存交换数据**——外存（硬盘）里的程序必须先调入主存才能运行
- 排除法：数据通路是 CPU 内部传输数据的通路（不存数据）；操作系统是管理程序，不是存储位置

> **原题 6**：（　）是程序运行时的存储位置，包括所需的数据。
>
> A. 数据通路　B. 主存　C. 硬盘　D. 操作系统
>
> 答案：**B**

### 1.2 计算机硬件的基本结构

错题：2、3、4、9、11、17

**① 寄存器的「存什么 ↔ 位数取决于」对照（错题 2）**

| 寄存器 | 存什么 | 位数取决于 |
|:---|:---|:---|
| IR 指令寄存器 | 当前**正在执行**的指令 | **指令字长** |
| PC 程序计数器 | **下一条**要执行指令的地址 | 地址码长度（主存地址位数） |
| MAR 地址寄存器 | 欲访问的存储单元地址 | **地址码长度** |
| MDR 数据寄存器 | 从存储单元读出／要写入的信息 | **存储字长** |
| 通用寄存器 | 操作数、运算结果 | 机器字长 |

- 第 2 题问的正是 IR 的位数 → **指令字长**；指令字长、机器字长、存储字长是三个不同的量，别互相顶替
- 顺带辨析：MAR 存"地址"、MDR 存"数据"；PC 存地址但不叫地址寄存器

> **原题 2**：指令寄存器的位数取决于（　）。
>
> A. 存储器的容量　B. 指令字长　C. 机器字长　D. 存储字长
>
> 答案：**B**

**② 两种译码器，归属完全不同（错题 3、9）**

- **指令译码**：由**控制单元**（操作码译码器）完成，属于 CPU → 第 3 题
- **地址译码**：由**存储器内部的地址译码器**完成，**不属于 CPU** → 第 9 题"CPU 不包括地址译码器"
- 干扰套路：把"译码"一律塞给 CPU；实际上数据和控制信号都不需要译码

> **原题 3**：计算机中（　）负责指令译码。
>
> A. 算术逻辑单元　B. 控制单元（或者操作码译码器）　C. 存储器译码电路　D. 输入／输出译码电路
>
> 答案：**B**
>
> **原题 9**：CPU 不包括（　）。
>
> A. 地址寄存器　B. 指令寄存器（IR）　C. 地址译码器　D. 通用寄存器
>
> 答案：**C**

**③ 运算器的成员与"外来户"（错题 11）**

- 运算器 = **ALU（核心）＋ 通用寄存器 ＋ 状态寄存器（PSW / 标志寄存器）**，承担算术与逻辑运算
- **地址寄存器（MAR）虽在 CPU 内，却不属于运算器，也不属于控制器**（第 9、11 题连考同一处坑）
- 数据总线是**总线**的组成部分，不是运算器的部件

> **原题 11**：在运算器中，不包含（　）。
>
> A. 状态寄存器　B. 数据总线　C. ALU　D. 地址寄存器
>
> 答案：**D**

**④ 存储部件的速度阶梯（错题 4）**

- 速度由慢到快：**外存 → 主存 → Cache → 寄存器**（第 4 题）
- 容量从小到大：寄存器 → Cache → 主存 → 外存
- 记忆锚点：**容量越小、越靠近 CPU → 越快、越贵**

> **原题 4**：在下列部件中，CPU 存取速度由慢到快的排列顺序正确的是（　）。
>
> A. 外存、主存、Cache、寄存器　B. 外存、主存、寄存器、Cache　C. 外存、Cache、寄存器、主存　D. 主存、Cache、寄存器、外存
>
> 答案：**A**

**⑤ 机器字长 = 一次能整体处理的二进制位数（错题 17）**

- 机器字长 4 字节 = 32 位 → **在 CPU 中能够作为一个整体处理 32 位二进制代码**
- 定义：机器字长是**数据运算的基本单位长度**，与 CPU 内通用寄存器位数、CPU 内部数据总线宽度一致
- 别误读成"能处理的数值最大为 4 位十进制数／4 位二进制数"——字长说的是**一次处理的位数**，不是数值范围或精度上限

> **原题 17**：若一台计算机的机器字长为 4 字节，则表明该机器（　）。
>
> A. 能处理的数值最大为 4 位十进制数　B. 能处理的数值最多为 4 位二进制数　C. 在 CPU 中能够作为一个整体处理 32 位的二进制代码　D. 在 CPU 中运算的结果最大为 2³²
>
> 答案：**C**

### 1.3 计算机系统层次结构

错题：3、5

**① "系统软件"的边界（错题 3）**

- 典型**系统软件**：操作系统、编译程序（语言处理程序）、连接程序（服务性程序）、数据库管理系统（DBMS）
- **"数据库系统"整体不属于系统软件**：它是"数据库 ＋ DBMS ＋ 应用系统 ＋ DBA"构成的系统，其中只有 DBMS 属于系统程序
- 典型**应用软件**：文本处理、会计电算化等面向具体任务的程序

> **原题 3**：下列（　）不属于系统软件。
>
> A. 数据库系统　B. 操作系统　C. 编译程序　D. 以上 3 种都属于系统程序
>
> 答案：**A**

**② "兼容"= 同一系列内的软／硬件通用性（错题 5）**

- 正确定义：兼容指计算机**软件或硬件**的通用性，**通常在同一系列的不同型号计算机之间**通用
- 三个错项的死因：A 说成"软件与硬件**之间**的通用性"；B 说"在**任何**计算机间可以通用"（把"同一系列"扩大成"任意"）；D 说"软件能通用、硬件不能通用"（兼容本就同时指软硬件）
- 联动记忆：系列机的基本特性之一是**指令系统向后兼容**——新机器能运行老机器的程序（同章 1.2 第 12 题）

> **原题 5**：下列关于"兼容"的叙述，正确的是（　）。
>
> A. 指计算机软件与硬件之间的通用性，通常在同一系列不同型号的计算机间存在
>
> B. 指计算机软件或硬件的通用性，即它们在任何计算机间可以通用
>
> C. 指计算机软件或硬件的通用性，通常在同一系列不同型号的计算机间通用
>
> D. 指软件在不同系列计算机中可以通用，而硬件不能通用
>
> 答案：**C**

### 1.4 冯诺依曼机模型与计算机系统的工作原理

错题：10、11、13（该节（一）选择题共 13 道，错 3 道）

**① 语言层次链：只有机器语言能被硬件直接执行（错题 10、13）**

- 单向链条：**高级语言 —编译→ 汇编语言 —汇编→ 机器语言**
- 三个高频错点：
  - "高级语言**汇编**后得到汇编语言" × ——高级语言经**编译**得到汇编语言；"汇编"专指汇编语言 → 机器语言这一步
  - "汇编语言能被硬件直接执行" × ——汇编语言是**符号化的机器级语言**，须由汇编程序翻译成机器语言才能执行
  - "高级语言是符号化的语言" × ——符号化语言特指用助记符书写的汇编语言；高级语言面向问题（过程），机器语言是 0/1 代码，两者都谈不上"符号化"
- 机器语言与汇编语言合称**机器级语言**；硬件只认机器语言

> **原题 10**：下列关于计算机语言的说法中，错误的是（　）。
>
> I. 高级语言比汇编语言更靠近机器语言。　II. 汇编语言和机器语言都能够被计算机硬件直接执行。　III. 高级语言汇编后得到汇编语言。
>
> A. I、II　B. II　C. II、III　D. I、II、III
>
> 答案：**D**（I、II、III 三项全错）
>
> **原题 13**：下列关于计算机语言的说法中，错误的是（　）。
>
> I. 机器语言是由 0 和 1 组成的代码语言，而高级语言和汇编语言是符号化的语言。　II. 机器语言和汇编语言是面向机器的语言，因此，能够被计算机硬件直接执行。　III. 高级语言需要编译成二进制机器指令后才能执行，而汇编语言因为与机器指令一一对应，所以不需要编译就可执行。
>
> A. I 和 II　B. I 和 III　C. II 和 III　D. I、II 和 III
>
> 答案：**D**（I 错在把高级语言也算作符号化语言）

**② 汇编语言的"机器相关性"（错题 11、13；第 13 题题干见本节 ①）**

- 汇编语言**面向机器、与机器结构强相关**（受寄存器位数、指令系统约束）→"汇编语言是一种与机器结构无关的编程语言" × ；**机器无关是高级语言**的特征
- 汇编语句与机器指令**一一对应**，"面向机器"的另一面
- 编译程序**必须了解机器结构和指令系统**，否则生成不了目标代码
- 正向表述要记牢：高级语言与低级语言**能写出功能等价的程序**；高级语言源程序、汇编语言源程序**都不能在机器上直接执行**

> **原题 11**：下列关于程序编写和执行方面的叙述中，错误的是（　）。
>
> A. 可用高级语言和低级语言编写出功能等价的程序
>
> B. 高级语言和汇编语言源程序都不能在机器上直接执行
>
> C. 编译程序必须了解机器结构和指令系统
>
> D. 汇编语言是一种与机器结构无关的编程语言
>
> 答案：**D**

### 2.2.1 运算电路与标志位生成

错题：2、7、8、9、13、14

**① 运算器的组成与 ALU 的输入输出（错题 2、13）**

- 运算器 = **ALU ＋ 通用寄存器组 ＋ 状态寄存器（PSW）**；**地址寄存器不属于运算器**（第 2 题 D 项），数据总线是**总线**的部件（第 2 题 B 项）——与 1.2 第 11 题同一处坑，连考两次
- **ALU 的输入不止"两个操作数"**：还有**功能选择控制信号**（指定做加、减、与、或）以及可能的进位输入；**输出也不止"和"**：还有运算结果和**状态标志**（进位、溢出、零）→ 第 13 题 C 项正是把 ALU 说成"两入一和"
- 补充：**ALU 属于组合逻辑电路**（无记忆，输出只由当前输入决定），74181 是典型的 4 位 ALU（13-A、13-D 正确；同考点还有第 4 题、第 12 题）

> **原题 2**：组成一个运算器需要多个部件，但下面所列（　）不是组成运算器的部件。
>
> A. 通用寄存器组　B. 数据总线　C. ALU　D. 地址寄存器
>
> 答案：**D**
>
> **原题 13**：关于 ALU 的叙述中，不正确的是（　）。
>
> A. ALU 是功能较强的组合电路
>
> B. ALU 的基本逻辑结构是超前进位加法器
>
> C. ALU 的输入只有参加运算的两个数，输出是两个数的和
>
> D. 74181 是典型的 4 位 ALU
>
> 答案：**C**

**② 模 2 补码 vs 模 4 补码（错题 7）**

| | 模 2 补码 | 模 4 补码 |
|:---|:---|:---|
| 别名 | 普通补码 | **变形补码 / 双符号位补码** |
| 模 | 2 | 4 |
| 符号位 | 1 位 | **2 位**（只在 ALU 中使用）／数据**存储、传送时仍只占 1 位** |
| 正常形态 | — | 两符号位相同：**00 为正、11 为负** |
| 溢出判断 | 最高位进位 ⊕ 次高位进位 = 1；或同号相加得异号 | **两个符号位不同**即溢出：**01 正溢出、10 负溢出** |
| 相对优势 | — | 判溢更直观，且能**区分正溢与负溢**（单符号位只能判"溢出了"） |

- 第 7 题逐项判断：**A 错**（模 4 补码更易检查**加减**运算的溢出问题，不是"乘除"）；**B 对**（存储只需一个符号位——任何**正确**的数值，其模 4 补码的两个符号位总是相同的）；**C、D 错**（不是"存储时占两个符号位"，也不是"在 ALU 中只有一位"）
- 记忆：**存 1 位、算 2 位**——数据存放与传送时按单符号位处理，只在把两个数送往 ALU 做加减时，才把该符号位的值同时送到双符号位
- 直观理解：两个符号位都参与运算，结果一旦超出可表示范围，数值部分会向符号位进位（或借位）把低位符号位翻转，于是两位不再相同

> **原题 7**：关于模 4 补码，下列说法正确的是（　）。
>
> A. 模 4 补码和模 2 补码不同，它更容易检查乘除运算中的溢出问题
>
> B. 每个模 4 补码存储时只需一个符号位
>
> C. 存储每个模 4 补码需要两个符号位
>
> D. 模 4 补码，在算术与逻辑部件中为一个符号位
>
> 答案：**B**

**③ 标志位（PSW）：SF / ZF / OF / CF（错题 8）**

- PSW（状态字寄存器）存两类内容：**状态标志**（进位 C、符号 SF、零 ZF、溢出 OF 等，大多数指令都会影响）与**控制标志**（中断标志、陷阱标志等）
- 四个常考标志一览：

| <nobr>标志</nobr> | 名称 | 置 1 的条件 | 用来判断谁溢出 |
|:---|:---|:---|:---|
| CF | <nobr>进位 / 借位</nobr> | 加法有进位、减法有借位（CF = C ⊕ Sub，加法 Sub=0、减法 Sub=1） | **无符号数** |
| ZF | 零 | 运算结果各位全为 0 | 判"相等" |
| SF | 符号 | **结果最高有效位（MSB）= 1** | 有符号数的正负（无符号数时无参考价值） |
| OF | 溢出 | **最高位进位 ⊕ 次高位进位 = 1**（同号相加得异号） | **有符号数** |

- 第 8 题答案 **D（不能确定）**：SF 只看结果的最高有效位，**SF=1 只说明该位为 1**——把它当有符号数解释时是负数，当无符号数时 SF 没有意义，所以推不出"正数／负数／非正数"
- 联动：本节综合应用题 1 就是"做完加减法后读 SF、ZF、OF、CF"，判溢口诀——**无符号数看 CF，有符号数看 OF**（第 15 题：次高位进位与最高位进位相异即为溢出）

> **原题 8**：在 CPU 的状态字寄存器中，若符号标志位 SF 为"1"，表示运算结果是（　）。
>
> A. 正数　B. 负数　C. 非正数　D. 不能确定
>
> 答案：**D**

**④ 算术右移（错题 9）**

- 正确操作：**符号位不变**，其余各位依次右移 1 位，**被移出的最低位送入进位标志位 CF** → 第 9 题选 **B**
- 三种移位别混：**逻辑右移**（符号位填 0，A 项）、**算术右移**（保符号）、**循环右移**（最低位绕回最高位，D 项）
- 为什么算术右移要保持符号位：它等价于**除以 2**（向下取整），符号不变才对负数成立
- 记忆：**保符号、移数值、低位进 CF**

> **原题 9**：下列算术右移指令执行的操作选项中，正确的是（　）。
>
> A. 符号位填 0，顺序右移 1 位，最低位移至进位标志位
>
> B. 符号位不变，顺序右移 1 位，最低位移至进位标志位
>
> C. 符号位填 1，顺序右移 1 位，最低位移至进位标志位
>
> D. 进位标志位移至符号位，顺序右移 1 位，最低位移至进位标志位
>
> 答案：**B**

**⑤ 加法器的结构差异与"为什么慢"（错题 14；对照第 1、5、12 题）**

- **串行加法器**：只有一个全加器，进位**串行逐位传递**（低位 → 高位），位数越多加法时间越长
- **超前进位加法器（先行进位）**：根本目的是**快速传递进位信号**（第 1 题），代价是位数越多**高位电路越复杂**（14-B 正确）
- 第 14 题 D 项错在把"慢"归因于"串行加法器高位电路复杂"——**恰恰相反**，复杂的是超前进位加法器的高位电路
- 对照：串行运算器无先行进位，只能由低位到高位逐位运算（第 5 题）；加法器、ALU、译码器、数据选择器都是**组合逻辑电路**，而**移位寄存器、计数器是时序逻辑电路**（有记忆功能，第 12 题）

> **原题 14**：下列叙述中，不正确的是（　）。
>
> A. 串行加法器位数越多加法时间越长
>
> B. 超前进位加法器位数越多高位电路越复杂
>
> C. 串行加法器比超前进位加法器的加法时间长的原因是串行加法器进位串行传递
>
> D. 串行加法器比超前进位加法器的加法时间长的原因是串行加法器高位电路复杂
>
> 答案：**D**

### 2.3 整数的表示和运算

错题：4、19、23、36、38

**① 补码比大小用"码值法"（错题 4、19）**

- 核心结论（直接背）：**补码表示下，若符号位相同，则无符号码值越大、对应的真值越大**——两个负数比大小，直接比补码的**无符号值**，码值大的真值也大
- 落地步骤：先写出**边界值的补码**，再让未知位去"超过／小于"它：
  - 第 4 题 [x]补=1.x1x2x3x4，x > −1/2 ⟺ 码值大于 1.1000 ⟺ **x1 必须为 1，且 x2~x4 至少有一个为 1**（选 A）
  - 第 19 题 [x]补=1.x1x2x3x4x5，x < −16 ⟺ 码值小于 110000 ⟺ **x1 必须为 0，x2~x5 任意**（选 C）
- 方向别搞反：**负数补码"码值越大真值越大"**，所以"x 大于边界"取码值更大的形态、"x 小于边界"取码值更小的形态
- 快检技巧：代入**特殊值**排除——如对照第 17 题用 [−1]补 = 1.0000 直接排除含"x1 必须为 1"的选项（解析推荐的另解）

> **原题 4**：[x]补=1.x1x2x3x4，当满足下列（　）时，x > −1/2 成立。
>
> A. x1 必须为 1，x2~x4 至少有一个为 1
>
> B. x1 必须为 1，x2~x4 任意
>
> C. x1 必须为 0，x2~x4 至少有一个为 1
>
> D. x1 必须为 0，x2~x4 任意
>
> 答案：**A**
>
> **原题 19**：设 x 为整数，[x]补=1.x1x2x3x4x5，若要 x < −16，x1~x5 应满足的条件是（　）。
>
> A. x1~x5 至少有一个为 1
>
> B. x1 必须为 0，x2~x5 至少有一个为 1
>
> C. x1 必须为 0，x2~x5 任意
>
> D. x1 必须为 1，x2~x5 任意
>
> 答案：**C**

**② 算术移位的补位规则与左移溢出判断（错题 36、38；对照第 21、22、37 题）**

| 码制 | 正数补位 | 负数补位 |
|:---|:---|:---|
| 原码 | 补 0 | 补 0 |
| 补码 | 补 0 | **左移补 0，右移补 1** |
| 反码 | 补 0 | 补 1 |

- **补码左移溢出的判断**（第 36 题，答案 B）：设 [X]补 = X0X1…Xn（X0 为符号位、X1 为最高数位），**X0 ≠ X1 时左移必然溢出**
  - 等价说法一：把 X0X1 当作**双符号位**看，两位不同（01 或 10）即溢出
  - 等价说法二：**数值位最高位的进位与符号位的进位不同**则溢出
  - 直观：左移后 X1 顶到符号位旁，**移出的最高位与原符号位不同**就说明数值已经变形
- **双符号位移位**：**高位符号位不参加移位，只有低位符号位参加移位**（第 38 题 III 项、第 37 题）——如 DAH = 11 011010，算术左移得 10 110100 = B4H、算术右移得 11 101101 = EDH
- 第 38 题三项都正确（答案 D）：I 反码负数移位会丢有效位——左移丢高位使**结果出错**、右移丢低位只**损失精度**；II 补码左移的前提是"最高有效位与符号位相同"（与第 36 题同一条结论）；III 双符号位只有低符号位参与移位
- 呼应 2.2.1 ②：这里的"最高有效位与符号位相同"就是**模 4 补码两个符号位相同**的另一种说法

> **原题 36**：若 [X]补 = X0X1X2…Xn，其中 X0 为符号位，X1 为最高数位。若（　），则当补码左移时，将会发生溢出。
>
> A. X0 = X1　B. X0 ≠ X1　C. X1 = 0　D. X1 = 1
>
> 答案：**B**
>
> **原题 38**：下列关于各种移位的说法中正确的是（　）。
>
> I. 假设机器数采用反码表示，当机器数为负时，左移时最高数位丢 0，结果出错；右移时最低数位丢 0，影响精度
>
> II. 在算术移位的情况下，补码左移的前提条件是其原最高有效位与原符号位要相同
>
> III. 在算术移位的情况下，双符号位的移位操作中只有低符号位需要参加移位操作
>
> A. I、III　B. 只有 II　C. 只有 III　D. I、II、III
>
> 答案：**D**

**③ 补码表示唯一 ⟹ 绝对值与取反的等式（错题 23）**

- 题干：设 x* = |x|，[−x*]补 = [−x]补 何时成立？
- 分情况拆：
  - **x ≥ 0（含 0）**：x* = x，等式两边都是"取相反数后的补码"，**恒成立**
  - **x < 0**：x* = −x，等式变成 [x]补 = [−x]补 —— 一边是负数、一边是正数，而**补码的表示是唯一的**，二者不可能相等
- 所以成立范围是"x 为非负数"；选项 B"x 为正数"漏掉了 0，且题干问的是"**当且仅当**"（充要条件），单选 B 顶多算充分条件 → **选 D（以上说法都不对）**
- 记忆：**补码表示唯一**（0 的补码也唯一）——这就是"同一个补码不会对应两个真值"的底气，也是补码优于原码、反码的地方之一

> **原题 23**：设 x 为真值，x* 为其绝对值，满足 [−x*]补 = [−x]补，当且仅当（　）。
>
> A. x 任意　B. x 为正数　C. x 为负数　D. 以上说法都不对
>
> 答案：**D**

### 2.5 浮点数的表示和运算

错题：1、4、8、9、10、11、13、16、18、19、20、21、22、23、24（该节（一）选择题原书编号 1~24，但**跳过第 5 题**，实为 23 道，错 15 道）

**① 规格化判定：原码「同位」、补码「异位」，基数决定查几位（错题 1、9、10、20、21；对照第 2 题）**

- 共同的底层含义：规格化就是让**尾数的最高数值位成为有效数字**——原码要求它是 1，补码要求它与符号位相反；**基数越大，一位基数数字占的二进制位越多，要"顶住"的前几位就越不能同时无效**
- 判据按码制、基数分档（第 1、9、10、20、21 题全靠这张表）：

| <nobr>尾数格式</nobr> | 规格化形态 | 判据（谁与谁的关系） |
|:---|:---|:---|
| <nobr>原码（基 2）</nobr> | 正数 0.1xxx、负数 1.1xxx | **符号位 = 小数点后第 1 位**（都要为 1）→ 第 9 题选 B |
| <nobr>补码（基 2）</nobr> | 正数 0.1xxx、负数 1.0xxx | **符号位 ≠ 小数点后第 1 位** → 第 1 题选 C |
| <nobr>模 4 补码（基 8）</nobr> | 正数：数值位前 3 位不全为 0；负数：前 3 位不全为 1 | 基 8 ⇒ 一位八进制数占 3 位二进制 → 第 10 题选 C |
| <nobr>原码（基 4）</nobr> | 小数点后 2 位不全为 0 | 基 4 ⇒ 一位四进制数占 2 位二进制 → 第 21 题选 C |

- 第 1 题的死因：**判规格化与阶符无关**（A、D 只差"相同／相异"，补码必须取"相异"）；补码规格化也**不看数值位是否为 1**（B 项是原码的思路）
- 第 20 题逐个过：1.11000（负数、最高数值位 1 与符号位相同，不规格化）与 0.01110、0.01010（正数、最高数值位为 0，不规格化）都不合要求；只有 **1.00010**（负数、最高数值位 0 与符号位相异）是规格化数
- 第 9 题只需看尾数：1.011101 的原码规格化形式应为 1.1xxxx，故它不是规格化数（各选项阶码位数排印不一致，判规格化与此无关）
- 口诀：**原码同位、补码异位；基数 2^k 就查 k 位，全无效才不规格化**

> **原题 1**：若浮点数用补码表示，则判断运算结果为规格化数的方法是（　）。
>
> A. 阶符与数符相同，则为规格化数　B. 小数点后第一位为 1，则为规格化数　C. 数符与小数点后第 1 位数字相异，则为规格化数　D. 数符与小数点后第 1 位数字相同，则为规格化数
>
> 答案：**C**

> **原题 9**：某浮点机，采用规格化浮点数表示，阶码用移码表示（最高位代表符号位），尾数用原码表示。下列（　）的表示不是规格化浮点数。
>
> A. 11111111，1.100000　B. 0011111，1.011101　C. 1000001，0.111101　D. 0111111，0.100010
>
> 答案：**B**

> **原题 10**：设浮点数阶的基数为 8，尾数用模 4 补码表示。下列浮点数中（　）是规格化数。
>
> A. 11.111000　B. 00.000111　C. 11.101010　D. 11.111101
>
> 答案：**C**

> **原题 20**：若浮点数的尾数用补码表示，则下列（　）中的尾数是规格化数形式。
>
> A. 1.11000　B. 0.01110　C. 0.01010　D. 1.00010
>
> 答案：**D**

> **原题 21**：设浮点数的基数为 4，尾数用原码表示，则以下（　）是规格化的数。
>
> A. 1.001101　B. 0.001101　C. 1.011011　D. 0.000010
>
> 答案：**C**

**② 规格化只买「精度」，范围要靠阶码和基数（错题 16；对照第 6、7 题）**

- 第 16 题正解 **D：采用规格化浮点数最主要为了增加数据的表示精度**——位数固定时，规格化把有效数字顶到最高位，不浪费有效位；它**不扩大表示范围**（范围由阶码位数决定）
- 两条此消彼长（总位数固定，第 6、7 题的结论）：**阶码越长 → 范围越大、精度越低**；**基数越大 → 范围越大、精度越低（数变稀疏）**
- 第 7 题解析的三条"注意"要单独记住，别与前一句打架：
  - 基数大 ⇒ **对阶右移的可能性和次数都减少**，所以**运算中的精度损失反而更小、运算速度更快**
  - 基数大 ⇒ **可表示的数的个数更多**
  - 但题面问的是"可表示的数的**范围与精度**（相邻两数的间隔）" ⇒ 仍答**范围大、精度低**

> **原题 16**：采用规格化的浮点数最主要是为了（　）。
>
> A. 增加数据的表示范围　B. 方便浮点运算　C. 防止运算时数据溢出　D. 增加数据的表示精度
>
> 答案：**D**

**③ 对阶：只准「小阶向大阶看齐」（错题 11、19）**

- 规则：**阶码小的数把阶码加到与大阶相同，每加 1，其尾数算术右移 1 位**
- 于是只有一种合法动作：**阶码增大 → 尾数右移**；"阶码减小、尾数左移"在浮点加减中**不存在** → 第 11 题 A、B 项的死因，选 C
- 第 19 题：x 阶 > y 阶 ⇒ **把 y 的阶码扩大至与 x 相同，y 的尾数算术右移**（选 D），而不是缩小 x 的阶码
- 为什么不能反向：大阶数的尾数**左移会丢最高有效位（数值出错）**，小阶数尾数右移只丢低位（损失精度）——两害相权，宁可丢精度
- 口诀：**小阶追大阶、尾数向右移；宁可丢精度，不可丢高位**

> **原题 11**：下列关于对阶操作说法正确的是（　）。
>
> A. 在浮点加减运算的对阶操作中，若阶码减小，则尾数左移　B. 在浮点加减运算的对阶操作中，若阶码增大，则尾数右移；若阶码减小，则尾数左移　C. 在浮点加减运算的对阶操作中，若阶码增大，则尾数右移　D. 以上都不对
>
> 答案：**C**

> **原题 19**：计算机在进行浮点数的加减运算之前先进行对阶操作，若 x 的阶码大于 y 的阶码，则应将（　）。
>
> A. x 的阶码缩小至与 y 的阶码相同，且使 x 的尾数部分进行算术左移　B. x 的阶码缩小至与 y 的阶码相同，且使 x 的尾数部分进行算术右移　C. y 的阶码扩大至与 x 的阶码相同，且使 y 的尾数部分进行算术左移　D. y 的阶码扩大至与 x 的阶码相同，且使 y 的尾数部分进行算术右移
>
> 答案：**D**

**④ IEEE 754：隐藏位只服务规格化数（错题 4、13；对照第 12、14、15 题）**

- 第 4 题逐项（错误的只有 III，选 C）：
  - I **对**：可表示 **±∞**（阶码全 1、尾数全 0）
  - II **对**：**允许使用非规格化数**（阶码全 0、尾数非 0），用于表示 0 附近的数，此时隐含位按 0 处理
  - III **错**：隐藏位（最高位隐含 1）**只对规格化数使用**，并非"任何形式的浮点数"都要隐藏
  - IV **对**：32 位格式 = 1 位符号 + 8 位阶码（**偏移值 127 的移码**）+ 23 位尾数（**原码**）
- 第 13 题（64 位最小规格化负数，选 B）：
  - 格式：1 位符号 + 11 位阶码（**偏移值 1023**）+ 52 位尾数（隐含最高位 1）
  - 取法：**符号位取负 → 阶码取最大 → 尾数绝对值取最大** ⇒ −(2 − 2⁻⁵²) × 2⁺¹⁰²³
  - 干扰项解剖：C 项把最小阶对应值 2⁻¹⁰²⁴ 当答案（那是"绝对值最小"的量级）；D 项偏移量错用 2047（2047 是 11 位全 1，留给 ∞ 与 NaN）；A 项指数符号取反、尾数写成 2⁻⁵² 的反面
- 记忆：**尾数原码、阶码移码；单精度偏 127、双精度偏 1023；只有规格化数才藏最高位 1**

> **原题 4**：关于浮点数在 IEEE 754 标准中的规定，下列说法中错误的是（　）。
>
> I. 浮点数可以表示正无穷大和负无穷大两个值
>
> II. 如果需要，也允许使用非格式化的浮点数
>
> III. 对任何形式的浮点数都要求使用隐藏位技术
>
> IV. 对 32 位浮点数的阶码采用了偏移值为 127 的移码表示，尾数用原码表示
>
> A. I、III　B. II、III　C. 只有 III　D. I、III、IV
>
> 答案：**C**

> **原题 13**：在 IEEE 754 标准规定的 64 位浮点数格式中，符号位为 1 位，阶码为 11 位，尾数为 52 位，则它所能表示的最小规格化负数为（　）。
>
> A. −(2 − 2⁻⁵²) × 2⁻¹⁰²³　B. −(2 − 2⁻⁵²) × 2⁺¹⁰²³　C. −1 × 2⁻¹⁰²⁴　D. −(1 − 2⁻⁵²) × 2⁺²⁰⁴⁷
>
> 答案：**B**

**⑤ 左规补「形态不规格化」、右规补「尾数溢出」（错题 8、24）**

- 两种规格化的触发条件与动作完全不同：
  - **左规**：尾数**不符合规格化形态**（补码时符号位与最高数值位相同，如 1.10101）→ 尾数**左移**、阶码**减 1**，可移多次，直到规格化为止
  - **右规**：尾数**溢出**（双符号位呈 01 或 10）→ 尾数**右移**、阶码**加 1**，**最多一次**
- 第 8 题：2⁵ × 1.10101 的尾数 1.10101 为补码，最高数值位 1 与符号位相同 ⇒ 不合格，须**左移一位**变成 1.01010（选 C，阶码同时减 1）；B 项"右移规格化"是拿处理溢出的手段去解决形态问题
- 第 24 题四项全对（选 D），关键是把"阶码溢出"与"尾数溢出"彻底分开：
  - I 对：**对阶操作本身不会引起阶码上溢／下溢**（它只是把小阶对齐到大阶，不会越界）
  - II 对：**右规与尾数舍入都在加阶码 → 可能引起阶码上溢**
  - III 对：**左规在减阶码 → 可能引起阶码下溢**
  - IV 对：**尾数溢出时结果不一定溢出**（阶码还容得下就不算真溢出）
- 记忆：**形态不对就左规（减阶）、尾数溢出才右规（加阶）；阶码越界才算真溢出**
- 顺带定锚：浮点数的溢出**只看阶码**——阶码大于最大正阶码为上溢（要转入溢出处理），小于最小负阶码为下溢（按机器零处理，第 17 题）

> **原题 8**：在规格化浮点运算中，若某浮点数为 2⁵ × 1.10101，其中尾数为补码表示，则该数（　）。
>
> A. 不需规格化　B. 需右移规格化　C. 需将尾数左移一位规格化　D. 需将尾数左移两位规格化
>
> 答案：**C**

> **原题 24**：下列关于浮点数加减运算的叙述中，正确的是（　）。
>
> Ⅰ. 对阶操作不会引起阶码上溢或下溢
>
> Ⅱ. 右规和尾数舍入都可能引起阶码上溢
>
> Ⅲ. 左规时可能引起阶码下溢
>
> Ⅳ. 尾数溢出时结果不一定溢出
>
> A. Ⅱ和Ⅲ　B. Ⅰ、Ⅱ和Ⅳ　C. Ⅰ、Ⅲ和Ⅳ　D. 全部
>
> 答案：**D**

**⑥ 舍入只发生在「对阶」与「右规」（错题 23）**

- 第 23 题只有 V 正确（选 C），逐项死因：
  - I 错：**舍入是浮点数运算的概念**，定点数运算不产生舍入（没有阶码，也就没有"丢低位"这一步）
  - II、III、IV 错：需要舍入的场合是**对阶时的尾数右移**与**右规时的尾数右移**，共性都是"**右移丢低位**"；左规是左移、丢的是无效位，不需要舍入 ⇒ "只有左规要舍入""只有右规要舍入""左右规都要舍入"三种说法全错
  - V 对：**舍入不一定产生误差**（移出位恰为 0 时无误差，如把 11.00 舍成 11.0）
- 记忆：**舍入总跟在"右移"后面——对阶右移、右规右移；左规是左移，不舍入**

> **原题 23**：下列关于舍入的说法，正确的是（　）。
>
> I. 不仅仅只有浮点数需要舍入，定点数在运算时也可能要舍入
>
> II. 在浮点数舍入中，只有左规格化时可能要舍入
>
> III. 在浮点数舍入中，只有右规格化时可能要舍入
>
> IV. 在浮点数舍入中，左、右规格化均可能要舍入
>
> V. 舍入不一定产生误差
>
> A. I、III、V　B. I、II、V　C. V　D. I、IV
>
> 答案：**C**

**⑦ 最大正数 = 最大正尾数 × 2 的最大正阶码（错题 18）**

- 第 18 题：12 位 = 阶码 4 位（含 1 位阶符、补码）+ 尾数 8 位（含 1 位数符、补码、规格化）
  - 阶码最大正：0111 = **7**（**不是 1111**，4 位补码的 1111 是 −1）
  - 尾数最大正：0.1111111 = **1 − 2⁻⁷**（7 位数值位全 1）
  - 最大正数 = (1 − 2⁻⁷) × 2⁷ = **2⁷ − 1**（选 D）
- 通用套路：**求最大正数** → 阶码取最大正阶码、尾数取最大正尾数；**求绝对值最大的负数** → 阶码取最大正阶码、尾数取绝对值最大的负尾数
- 陷阱：尾数用**补码**时，绝对值最大的负尾数是 −1（写作 1.0000000），不是 −(1 − 2⁻⁷)

> **原题 18**：设浮点数共 12 位。其中阶码含 1 位阶符共 4 位，以 2 为底，补码表示；尾数含 1 位数符共 8 位，补码表示，规格化。则该浮点数所能表示的最大正数是（　）。
>
> A. 2⁷　B. 2⁸　C. 2⁸ − 1　D. 2⁷ − 1
>
> 答案：**D**

**⑧ 移位三条款（错题 22；题干与结论同 2.3 第 38 题）**

- 本题与 **2.3 第 38 题**是同一道题（题干、选项、答案完全相同，选 D），三条结论见本节 2.3 ②：反码负数左移丢高位（结果出错）、右移丢低位（损失精度）；补码左移的前提是**最高有效位与符号位相同**；**双符号位只有低符号位参加移位**
- 解析给的验证实例（顺手记下）：5 位反码 10010 = −13 → 右移得 11001 = −6（损精度）、左移得 10101 = −10（数值出错）
- 与本章的衔接：补码左移"最高有效位与符号位相同"这一条，正是 2.2.1 的**模 4 补码两个符号位相同**、以及本节 ⑤ 中**左规前提**的同一种说法

> **原题 22**：下列关于各种移位的说法正确的是（　）。
>
> I. 假设机器数采用反码表示，当机器数为负时，左移时最高数位丢 0，结果出错；右移时最低数位丢 0，影响精度
>
> II. 在算术移位的情况下，补码左移的前提条件是其原最高有效位与原符号位要相同
>
> III. 在算术移位的情况下，双符号位的移位操作只有低符号位需要参加移位操作
>
> A. I、III　B. 仅 II　C. 只有 III　D. I、II、III
>
> 答案：**D**（题干与结论同 2.3 第 38 题，见 2.3 ②）

### 待整理章节

（后续小节错题按同样格式追加于此）

2.5（二）2，3，8

